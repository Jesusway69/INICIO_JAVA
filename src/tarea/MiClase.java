package tarea;

public class MiClase extends ImplementacionTarea {
    
    
    
    public double potencia(double base, double exponente) {
        return  Math.pow(base, exponente);
    }
}
